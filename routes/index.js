const authRouters = require("./auth");
const dashboardRouters = require("./dashboard");
const trafficLightRouters = require("./trafficLight");
const loggerRouters = require("./logger");

module.exports = {
	authRouters,
	dashboardRouters,
	trafficLightRouters,
	loggerRouters,
};
